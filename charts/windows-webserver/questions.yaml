questions:
- variable: image.tag
  default: "ltsc2019"
  description: "The Windows Server version of Windows hosts in this cluster (only one can be selected)."
  label: "Windows Server Version"
  type: enum
  group: "Windows"
  options:
  - "ltsc2019"
  - "ltsc2022"
